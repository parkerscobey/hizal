# Server
PORT=8080
ENV=development
# Comma-separated list or '*' (for local-only use)
CORS_ALLOW_ORIGINS=http://localhost:5173

# Database (PostgreSQL with pgvector)
DATABASE_URL=postgres://user:password@localhost:5434/winnow?sslmode=disable

# OpenAI
OPENAI_API_KEY=sk-...
